# Plantilla de firma de release. Copiar a `android/keystore.properties` (gitignored)
# y completar con los datos reales del upload keystore. NUNCA commitear el archivo real.
# Generar el keystore: ver docs/ANDROID_RELEASE.md §4.
storeFile=/ruta/segura/alyto-upload.jks
storePassword=CAMBIAR
keyAlias=alyto-upload
keyPassword=CAMBIAR
